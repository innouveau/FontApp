import './Identity.scss';
import logo from './logo_fontapp.jpg';

function Identity() {
    return (
        <div className="Identity">
            <img className="Identity__logo" src={logo}/>
        </div>
    );
}

export default Identity;